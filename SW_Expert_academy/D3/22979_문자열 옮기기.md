# 내 코드
```py
T = int(input())
for stage in range(1, T+1):
    sen = input()
    n = int(input())
    order = list(map(int, input().split()))
    sum_of_order = sum(order)
    move = abs(sum_of_order) % len(sen)
    if sum_of_order < 0:
        print(sen[-move:] + sen[:-move])
    else:
        print(sen[move:] + sen[:move])
```
# GPT 피드백
## 🤏 살짝 아쉬운 점 / 개선 포인트
### 1. 파이썬 나머지 연산으로 더 단순하게 가능
파이썬에서는 음수도 %로 바로 “양수 회전량”을 구할 수 있어:
```py
shift = sum(order) % len(sen)
result = sen[shift:] + sen[:shift]  # 이거면 끝
```
왜나면:<br>
- 예를 들어 길이 5인 문자열에서 오른쪽 1칸(-1) 회전은
왼쪽으로 4칸(+4) 회전과 같음.
- 파이썬에서 -1 % 5 == 4라서,
sum(order) % len(sen)로 곧바로 “왼쪽 회전 기준 칸 수”를 얻을 수 있다.
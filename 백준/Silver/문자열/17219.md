# 내 코드
```py
from collections import defaultdict
N, M = map(int, input().split())
memo = defaultdict()
for _ in range(N):
    site, pw = input().split()
    memo[site] = pw
for _ in range(M):
    site = input()
    print(memo[site])
```
# GPT 피드백
- good
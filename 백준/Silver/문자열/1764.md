# 내 코드
```py
import sys
from collections import Counter
input = sys.stdin.readline
N, M = map(int, (input().rstrip()).split())
check = []
for _ in range(N+M):
    check.append(input().rstrip())
result = Counter(check)
name = sorted(result)
people = []
for key in name:
    if result[key] == 2:
        people.append(key)
print(len(people))
for i in people:
    print(i)
```
- 시간복잡도: O(nlogn)

# GPT 피드백
## 살짝 아쉬운 점 / 개선 포인트
### (1) 굳이 check 리스트 전체를 들고 있을 필요는 없음
지금은:
1. 모든 이름을 check에 넣고
2. 한 번에 Counter(check)로 세는 구조인데,

메모리 덜 쓰고 싶으면 읽으면서 바로 세도 돼:
```py
from collections import Counter
import sys
input = sys.stdin.readline

N, M = map(int, input().split())
result = Counter()

for _ in range(N + M):
    name = input().rstrip()
    result[name] += 1
```
### (2) 문제 특성을 이용하면 Counter 없이도 됨
문제에서 각 명단 안에는 중복이 없다고 했으니까,
이럴 땐 “듣도 못한 사람 set”과 “보도 못한 사람 set”의 교집합만 구하면 끝이야.
```py
import sys
input = sys.stdin.readline

N, M = map(int, input().split())

heard = set()
for _ in range(N):
    heard.add(input().strip())

seen = set()
for _ in range(M):
    seen.add(input().strip())

people = sorted(heard & seen)   # 교집합

print(len(people))
print("\n".join(people))
```
### (3) 자잘한 스타일
- name = sorted(result) 는 name = sorted(result.keys())와 같음 → OK
- for i in people: print(i) 대신
    print("\n".join(people)) 쓰면 I/O 호출 수가 줄어서 미세하게 더 빠름.

## Dict 정렬?
Counter는 dict의 서브클래스라 정렬해도 기본적으로 list로 나오고,
정렬된 상태의 dict로 만들고 싶으면 두 가지 방법이 있다.
### 1. collections.OrderedDict 사용 (가장 명확한 방법)
```py
from collections import Counter, OrderedDict

result = Counter(check)
ordered = OrderedDict(sorted(result.items()))
```
- result.items() → (key, value) 튜플 목록
- sorted(...) → key 기준 정렬
- OrderedDict(...) → 정렬된 순서 그대로 유지되는 dict

👉 ordered는 정렬된 dict가 유지됨.

### 2. 일반 dict 로도 가능 (파이썬 3.7+ 에서 dict는 ordered)
```py
result = Counter(check)
ordered = dict(sorted(result.items()))

```